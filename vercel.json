{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "prisma generate && (if [ -n \"$DATABASE_URL\" ]; then prisma migrate deploy; else echo 'Pas de DATABASE_URL — migrations ignorées (connecter Neon à Vercel)'; fi) && next build"
}
