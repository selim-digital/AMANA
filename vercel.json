{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "prisma generate && (if [ -n \"$DATABASE_URL\" ]; then prisma db push --accept-data-loss; else echo 'Pas de DATABASE_URL — schema non synchronise (connecter Neon a Vercel)'; fi) && next build"
}
